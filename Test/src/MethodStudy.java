class MethodStudy {
		//메소드 선언
		/* 최대값 반
		static int max(int a, int b, int c) { -> int 반환형 / max 메소드명 / int형인 a,b,c 가인
			int max = a;
			if(b > max) max = b;
			if(c > max) max = c;
			return max;
		}
		*/
}
